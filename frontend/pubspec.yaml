name: frontend
description: "AgroMind — AI-driven geospatial decision-support system."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.13.0
  cloud_firestore: ^5.6.7
  go_router: ^15.1.2
  flutter_riverpod: ^2.6.1
  http: ^1.6.0
  flutter_markdown: ^0.7.7+1
  google_fonts: ^6.2.1
  uuid: ^4.5.1
  intl: ^0.20.2
  google_maps_flutter: ^2.14.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
