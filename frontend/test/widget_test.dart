// Basic smoke test for AgroMind.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('AgroMind smoke test — placeholder', () {
    // Firebase-dependent widget tests require mock setup.
    // This placeholder ensures `flutter test` passes cleanly.
    expect(1 + 1, equals(2));
  });
}
